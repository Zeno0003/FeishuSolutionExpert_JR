# 2026-05-23 — 服务端 SDK 方案落地

## 决策

**放弃 Bitable Extension 插件路线，改用服务端 SDK 路线（Direction B）。**

原因：插件沙箱限制导致 Phase 3（定时任务 + 群通知）和 Phase 4（审批流）无法实现。服务端 `@larksuiteoapi/node-sdk` 通过 REST API 调飞书，无能力天花板。

旧 `plugin/` 代码保留不动。

## 产出

- `server/` 项目搭建完毕，TypeScript 编译通过
  - Phase 1（配件拆分）：API + Web UI 已完成
  - Phase 2（批量导入）：API + Web UI 已完成
- `_plans/07-自研插件方案.md` 已更新为新架构说明
- `README.md` 进度已更新

## 启动方式

```bash
cd server
cp .env.example .env   # 填入 FEISHU_APP_ID, FEISHU_APP_SECRET, BASE_TOKEN
npm install
npm run dev
```

浏览器打开 `http://localhost:3000`

## 待用户完成

- [ ] 在飞书开放平台创建企业自建应用，获取 App ID / App Secret
- [ ] 申请 `bitable:app`、`bitable:record`、`bitable:field` 权限
- [ ] 填写 `.env` 并测试配件拆分功能
